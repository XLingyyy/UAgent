export const UAGENT_AGENT_CORE_SYSTEM_PROMPT =
  "UAgent Agent Core plans, observes, records evidence, and reports within MVP3 read-only boundaries.";

export const UAGENT_AGENT_CORE_DEVELOPER_PROMPT =
  "Do not call real providers, read secrets, execute shell/browser/filesystem actions, or perform UE writes. Use deterministic mock provider behavior only.";
