/**
 * Shared UI components directory.
 *
 * Reusable presentational components (buttons, inputs, icons, etc.)
 * will be added here in subsequent UI tasks. Kept as a stable import
 * surface so the shell layer can reference it early.
 */
export {};
