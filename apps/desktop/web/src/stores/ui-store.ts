/**
 * UI store placeholder.
 *
 * This module will be replaced by a Zustand store layer in a later
 * task (UI roadmap step 10). For now it exports the context hook
 * used by the UI shell so components have a stable import surface.
 */

export { useUI } from "../app/providers";
